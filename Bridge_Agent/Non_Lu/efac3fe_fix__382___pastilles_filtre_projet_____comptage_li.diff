efac3fe

commit efac3fe
Author: Athanatos123 <alain.delree@gmail.com>
Date:   Thu Aug 6 17:50:20 2026 +0200

    fix #382 : pastilles filtre projet — comptage limité a la portee N
    
    - data-projet (construireBoutonsFiltre) et ordre d'appel de
      majPastillesFiltres() apres population de listeIssuesResultats etaient
      deja corrects (verifies, aucune modification necessaire).
    - Cause reelle : majPastillesFiltres() comptait sur toute
      listeIssuesResultats sans respecter le champ "par projet : N"
      (limiteIssuesProjet()), pouvant gonfler la pastille au-dela de ce
      qui est visible quand N est abaisse sans rafraichir. Limite desormais
      le comptage aux N premieres issues de chaque projet.

diff --git a/CHANGELOG.md b/CHANGELOG.md
index 0179367..31f96ce 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -9,6 +9,26 @@ milliers de caractères sur une seule ligne logique, coûteux à relire et
 
 Convention d'ajout : voir §10 de `BRIDGE_AGENT_DOC.md`.
 
+## 6 août 2026 — issue #382
+
+Correctif suite #381 : les pastilles de notification des boutons de filtre
+projet ne respectaient pas la portée courante.
+- Investigation des deux causes suspectées par l'issue : l'attribut
+  `data-projet` posé sur `.filtre-projet` (`construireBoutonsFiltre`) et
+  l'ordre d'appel de `majPastillesFiltres()` après le peuplement de
+  `listeIssuesResultats` (`appliquerListeIssues`, `rendreListeIssues`,
+  `remplacerLigneIssue`) étaient déjà corrects — aucune des deux ne causait
+  l'absence de pastilles.
+- **Cause réelle** : `majPastillesFiltres()` comptait sur la totalité de
+  `listeIssuesResultats`, sans tenir compte de la portée du champ
+  « par projet : N » (`limiteIssuesProjet()`) — un compte pouvant dépasser ce
+  que l'utilisateur voit réellement dans la liste dès que N est abaissé sans
+  cliquer sur ↻ (la saisie seule n'invalide que le cache, pas les données déjà
+  en mémoire). Le comptage se limite désormais aux N premières issues de
+  chaque projet (même lecture de la limite que le téléchargement), avant
+  d'appliquer le filtre ouvert/ni-done/ni-needs-human.
+- Fichier touché : `static/js/app.js` (`majPastillesFiltres`).
+
 ## 6 août 2026 — issue #381
 
 Améliorations de confort du panneau flottant (suite #380), sans changement
diff --git a/static/js/app.js b/static/js/app.js
index ec29904..5acdc97 100644
--- a/static/js/app.js
+++ b/static/js/app.js
@@ -997,9 +997,20 @@ function cocherToutesVisibles() {
 // listeIssuesResultats (déjà en mémoire) — aucun fetch réseau. Appelée à
 // chaque (re)construction des boutons et à chaque mise à jour de la liste
 // (rendreListeIssues, remplacerLigneIssue).
+// Portée respectée (issue #382) : ne compte que dans les N premières issues
+// de CHAQUE projet (N = limiteIssuesProjet(), le champ « par projet : N »),
+// même lecture de la limite que celle qui borne le téléchargement — sinon un
+// cache plus profond que la limite couramment affichée (N abaissé sans clic
+// sur ↻, cf. commentaire de limiteIssuesProjet) gonflerait la pastille
+// au-delà de ce que l'utilisateur voit réellement dans la liste.
 function majPastillesFiltres() {
+  const limite = limiteIssuesProjet();
+  const vus = {};
   const comptes = {};
   listeIssuesResultats.forEach(it => {
+    const v = vus[it.projet] || 0;
+    if (v >= limite) return;
+    vus[it.projet] = v + 1;
     if ((it.state || '').toUpperCase() !== 'OPEN') return;
     const noms = (it.labels || []).map(l => ((l && l.name) || l || '').toLowerCase());
     if (noms.includes('done') || noms.includes('needs-human')) return;
